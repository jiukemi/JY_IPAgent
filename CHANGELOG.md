# 更新日志

本文件使用中文记录面向用户的版本说明。发版时 GitHub / Gitee Release 正文与此处保持一致。

## 0.1.26 — 2026-09-05

> **已装好口播的用户可跳过。** 上一版提示「桌面已生成脚本」但部分机器桌面并无文件——非常抱歉。

### 修复
- Docker 安装脚本改为英文名 **`JY-Install-Docker.cmd`**，同时写入：系统桌面、下载文件夹、所选安装盘、LOCALAPPDATA。
- **写入后必须 `exists` 校验通过才提示**；失败就明确报错，不再假装已生成。
- 用系统对话框显示真实完整路径，并在资源管理器中选中该文件。

## 0.1.25 — 2026-09-05

> **已装好口播的用户可跳过。** 本版只改 Docker 安装引导：之前失败提示指向隐藏路径/英文文件名，小白找不到——非常抱歉。

### 修复
- Docker「安装到所选盘」：在**桌面**生成可见脚本「九易AI-安装Docker.cmd」，失败/成功都会在资源管理器里定位该文件；UAC 用 Shell.Application 请求管理员。不再要求去 `%LOCALAPPDATA%` 找英文 `install_docker_….cmd`。

## 0.1.24 — 2026-09-05

> **已装好口播的用户可跳过。**

### 修复
- Docker 安装：改为直接对安装包提权（不再依赖对 `.cmd` 的 RunAs），并改写失败提示——说明「包已找到，是管理员确认未通过」，失败时自动打开脚本文件夹方便右键运行。

## 0.1.23 — 2026-09-05

> **已装好口播的用户可跳过。**

### 修复
- Docker 官方安装包名带空格（`Docker Desktop Installer.exe`）会导致安装脚本异常：安装前自动复制为无空格路径 `DockerDesktopInstaller.exe` 再执行。

## 0.1.22 — 2026-09-05

> **已装好口播且能正常使用的用户可跳过。** 本版修复安装引导与首次启动卡顿；造成不便非常抱歉。

### 修复
- Docker「安装到所选盘」改为由桌面壳弹出 UAC（并生成可手动右键运行的 .cmd），避免只提示不弹窗。
- 首次启动卡在约 96%：健康检查绕过系统代理，并避免把「即将监听」日志误判为已就绪；失效梯子/代理时给出明确中文提示。

## 0.1.21 — 2026-09-05

> **已装好口播（HeyGem）且能正常生成的用户，可跳过 0.1.20 / 0.1.21。**  
> 这两版主要优化「尚未装好 Docker / 口播」的安装引导；对你现有环境无强制升级必要。  
> 安装引导反复调整给大家添麻烦了，**非常抱歉**。

### 口播 / Docker
- 纠正「一键从官网下载 Docker Desktop」在国内常失败（retrieval incomplete）的问题：改为 **先自行下载安装包 → 扫描/选路径 → 选盘安装**。
- 官网下载降为明确标注「国内常失败」的备用项；失败提示改为中文说明。

## 0.1.20 — 2026-09-05

### 口播 / Docker
- Docker 验收改为：本机 `docker info` 成功即可；个人使用通常无需注册 Docker Hub，登录窗可跳过。
- 口播安装向导支持 **选盘 + 一键安装 Docker Desktop**（下载安装包并弹管理员确认），程序与镜像数据装到所选盘（如 `D:\Docker\`），避免官网图形安装只能装 C 盘。
- 官网下载降为备用入口；用户无需自行运行终端命令。

### 文档
- 用户手册、夸克加速包说明同步上述安装与验收说明。

## 0.1.19 — 2026-09-05

- 更新夸克口播加速包分享链接（通用 / RTX50 分开，含提取码）。

## 0.1.18 — 2026-09-05

- 清理 FunASR 转写结果中的版本/更新提示杂讯。
- Whisper 长时间 CPU 识别增加进度心跳，减少「卡在 35%」误判。

## 0.1.17 — 2026-09-05

- 兼容旧版 Whisper / FunASR 不支持 `--out` 的情况。
- 去掉过时的「组件中心」文案。

## 0.1.16 — 2026-09-05

- CDN 提取会接管卡住的浏览器登录配置锁，避免一直占着无法提取。

## 0.1.15 — 2026-09-05

- 修复 Windows 路径写入 `config.yaml` 导致 YAML 解析失败的问题，并支持自动修复损坏配置。
